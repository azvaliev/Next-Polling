import {
	Html, Head, Main, NextScript,
} from 'next/document';

const Document = () => (
	<Html>
		<Head />
		<body className="dark">
			<Main />
			<NextScript />
		</body>
	</Html>
);

export default Document;
